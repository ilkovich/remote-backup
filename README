
= Michael Fraser

A script for backing up remote databases using curl and phpMyAdmin.  This is useful when you don't have shell access to your web server.

This has been tested on phpMyAdmin - 2.9.2.

Simply set the following variables at the top of the script and you are away.

USERNAME
PASSWORD
PROTOCOL
DOMAIN
DATABASE
OUPUT_DIR
COMPRESSION
METHOD

= Daniel Ilkovich

Added automatic push to s3 using s3cmd 
Use nodejs to get table list (could probably do this with curl, but I had the code already)
